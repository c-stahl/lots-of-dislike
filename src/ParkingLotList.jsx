import React, { useEffect, useState } from "react";

function App({data}) {
  // Main component
  return !data ? null : (
    <div >
      {/* TODO: if data were accessible, use https://github.com/mbrn/material-table
       * for a quick, flexible way to display it
       */}
    </div>
  );
}

export default App;
